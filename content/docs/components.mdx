---
id: 3
title: "Components"
description: "Browse every component grouped by category and jump to its documentation URL."
url: "/components"
---

The component docs are now grouped into focused categories such as forms, overlays, navigation, feedback, actions, and display. Each page still keeps the same `/docs/components/...` URL, so existing links stay stable while the content stays easier to scan.

<ComponentsNavigation />
